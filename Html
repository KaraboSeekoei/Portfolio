<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Karabo Seekoei's Portfolio</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            color: #fff;
            margin: 0;
            padding: 0;
            background: url('Untitled design.png') no-repeat center center fixed;
            background-size: cover;
        }

        header {
            text-align: left;
            animation: fadeIn 1s ease-out;
            padding: 20px;
        }

        nav {
            text-align: right;
            padding: 10px 0;
            margin-top: -20px; /* Adjusted margin-top */
        }

        nav a {
            color: #fff;
            text-decoration: none;
            padding: 0 10px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 20px;
            animation: slideIn 1s ease-out;
            display: flex;
            flex-direction: row-reverse;
            align-items: center;
        }

        .profile-img {
            width: 200px;
            margin-left: 20px;
            animation: fadeIn 1s ease-out;
        }

        .about-me {
            flex-grow: 1;
            text-align: justify;
            width: 100%;
            font-family: Georgia, serif; /* Set font to Georgia */
            color: #ffffff; /* Change text color to brown */
        }

        #date-time {
            text-align: center;
            margin-top: 20px;
            font-size: 18px;
        }

        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateY(-50px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }

        @keyframes underlineMotion {
            0% {
                text-decoration: underline;
                transform: translateX(0);
            }
            50% {
                text-decoration: none;
                transform: translateX(5px);
            }
            100% {
                text-decoration: underline;
                transform: translateX(0);
            }
        }

        h1 span,
        footer span {
            animation: underlineMotion 2s infinite;
            display: inline-block;
        }
    </style>
</head>
<body>
    <header>
        <h1><span>KaySay</span></h1>
        <nav>   
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Projects</a>
            <a href="#">Contact</a>
        </nav>
    </header>
    
    <div class="container">
        <div class="about-me">
            <h2>Hey, I'm Karabo</h2>
            <p>Welcome to my portfolio!</p>
                <p>I am a dedicated student at WeThinkCode, mastering programming languages like Python, Java, HTML, and C#. My academic journey focuses on acquiring versatile skills and understanding tech principles. This portfolio showcases my projects and proficiency.
                I'm passionate about applying technology to real-world scenarios and seek opportunities to make a meaningful impact. 
                Explore my portfolio to witness my dedication.
                Let's connect and explore the endless possibilities of technology together!</p>
        </div>
    </div>

    <div id="date-time"></div>

    

    <script>
        // Function to update date and time
        function updateDateTime() {
            var now = new Date();
            var date = now.toDateString();
            var time = now.toLocaleTimeString();
            var dateTimeString = date + " | " + time;
            document.getElementById("date-time").textContent = dateTimeString;
        }

        // Update date and time immediately when the page loads
        updateDateTime();

        // Update date and time every second
        setInterval(updateDateTime, 1000);
    </script>
   
</body>
</html>
